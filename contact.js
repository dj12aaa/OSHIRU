(async()=>{const box=document.getElementById('contactState');if(!box)return;try{const controller=new AbortController();const timer=setTimeout(()=>controller.abort(),5000);const r=await fetch('/api/contact-email',{signal:controller.signal,headers:{accept:'application/json'}});clearTimeout(timer);if(!r.ok)throw new Error('contact_unavailable');const d=await r.json();if(d.contactEmail){const a=document.createElement('a');a.href=`mailto:${d.contactEmail}`;a.textContent=d.contactEmail;box.replaceChildren(document.createTextNode('お問い合わせ先：'),a)}else{box.textContent='公開用の問い合わせメールアドレスは現在準備中です。設定後にこのページへ表示されます。'}}catch{box.textContent='お問い合わせ窓口を読み込めませんでした。時間をおいて再度お試しください。'}})();
